{
  "name": "@cafeos/node-agent",
  "version": "0.1.0",
  "private": true,
  "description": "CafeOS Edge node-agent — health reporting + allowlisted remote actions",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "tsx watch src/main.ts",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cafeos/config": "workspace:*",
    "@cafeos/shared": "workspace:*",
    "express": "^4.21.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
